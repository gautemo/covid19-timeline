name: Data Source

on:
  push:
    branches: master
  schedule:
    - cron: '0 16 * * *'

jobs:
  api-test:
    runs-on: ubuntu-latest
    if: github.repository == 'gautemo/covid19-timeline'

    steps:
    - uses: actions/checkout@v2
    - name: Fetch Data to File
      uses: gautemo/fetch-api-data-action@1.0.5
      with:
        url: 'https://europe-west1-covid-19-map-spread.cloudfunctions.net/getCovidDataxx'
        file: 'cloud-functions/backupData.json'
    - name: Commit file to repo
      run: |
        git status
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git commit -m "GitHub Action, daily backup of API data" -a
        git push https://gautemo:${{ secrets.MY_GITHUB_TOKEN }}@github.com/gautemo/covid19-timeline.git
